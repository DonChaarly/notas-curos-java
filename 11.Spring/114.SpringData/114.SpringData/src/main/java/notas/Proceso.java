package notas;

public class Proceso {
    
    /*
    
    Los comentarios con el numero 114 son los de esta clase
    
    Se agregaron mas dependencias(librerias) al pom.xml
        Se agregaron la librerias de SpringData y MySql
        Para agregar mas dependencias se pueden escribir directamente o 
        utilizar el wizard de spirngboot dando clik derecho, insert code y 
        spring boot dependencies
        Tambien se puede seleccionar la libreria de H2 Database para utilizar 
        una base de datos en memoria por si se quiere trabajar con pruebas
    Se hace clean&build del proyecto pero marca error ya que no teniamos configurada la conexion a la base de datos en el application.properties
    Se agrego la configuracion de conexion a MySql en el application.properties
    se movio la clase ControladorInicio a la carpeta web
    Se limpio el codigo de la clase ControlInicio y se agrego nuevo codigo 
    Se modifico la clase Persona
    Se agrego la interface IPersonaDao
        
        
            
    */
    
}               