package notas;

public class Proceso {
    
    /*
        Se colocaron algunas anotaciones en el pom.xml
        se colocaron anotaciones en la clase HolaSpringApplicati
            Debido a que spring es un contenedor de clases java, asi que para que spring reconozca las clases 
            que agregaremos a la aplicacion estas deben estar dentro del mismo paquete donde se encuentra nuestra clase
            que contiene la anotacion de SpringBootApplication
        Se agrego una clase java ControladorInicio el cual sera un controlador de tipo REST 
        Se agregaron algunas configuraciones el archivo application.properties que esta en othersources
        Las modificaciones que se hicieron en el archivo de properties fueron en orden
            
            Si se quiere cambiar algo del login lo que esta en comentarios

    */
    
}               