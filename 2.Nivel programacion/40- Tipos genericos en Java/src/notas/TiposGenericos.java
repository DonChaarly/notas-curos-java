package notas;
public class TiposGenericos {
    
    /*
                Tipos genericos que se pueden utilizar
    E -> Element(Utilizado generalmente por el framework de Colecciones de Java)
    K -> Key (Llave, utilizado en mapas)
    N -> Number ( utilizado para numeros)
    T -> Type (representa un tipo, es decir una clase)
    V -> Value (representa un valor, tambien se usa en ma
    S,U,V, etc -> Usado para representar otros tipos.
    
    
        Estos tipos son como un tipo generico en Java, porque no especificamos su tipo
    hasta que creemos un objeto de la clase, ahi es cuando especificamos su tipo.
    
    */
    
}
