public class clasesJava {
    public static void main (String args[]){
        /*
        Notas:
        Las clases son como plantillas que poseen atributos y metodos
        Los objetos son lo que se hace con esas plantillas (instancia de una clase)
        donde ya poseen un nombre y atributos personalizados
        Para crear una clase se da click derechp en source packages -> new -> java class
        y se nombra la clase y se puede indicar el package donde se almacenara.
        para poder acceder a las clases desde otros archivos se coloca la palabra public antes de la clase en el codigo
        public class Person
        public (indicamos que la clase es publica) class (se tiene que colocar esta palabra reservada) Persona (la primera letra debe ser mayuscula)
        */
    }
    
}
