package notas;

public class ConfiguracionDeAplicacion {
    
    /*
    Para empezar
    
    1. Ir a pom.xml en project Files y agregar lo que se indica ahi 
    2. Se agrego el folder y la clase de beans.configuracion y ConfiguracionJSF (Esto se tiene que hacer para la version de glassfish 5.0)
    3. Se agrega una carpeta en Files a la altura de scr -> main llamada resources
    4. En Projects dentro de la misma carpeta en other Sources se agrega un archivo other y se selecciona XML -> XML Document, se llamara log4j2 y se selecciona la primera opcion
    5. Se agrego un archivo index xhtml para ver que todo este funcionando
    6. se agrego el archivo web.xml en la carpeta de WEB-INF
    
    */
    
}
